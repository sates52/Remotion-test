# Project Rules & Guidelines

## Git & Deployment Protocol
- **DO NOT push to render worker remotes (ender-worker-*) during regular/core work.**
  - Workers receive code automatically during the render dispatch process.
  - Pushing to workers repeatedly during intermediate development is redundant and unnecessary.
- **Push ONLY to the main GitHub repository (origin)** when committing code, schema, config, or script changes.
